{
  "name": "tailwindcss-smooth-corners-monorepo",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r build",
    "changeset": "changeset",
    "dev": "pnpm --filter site dev",
    "lint": "pnpm -r lint",
    "release": "pnpm build && changeset publish",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.1",
    "@changesets/cli": "^2.29.6",
    "@types/node": "^24.6.0",
    "typescript": "^5.9.3"
  }
}
